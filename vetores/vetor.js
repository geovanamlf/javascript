let num = [5, 8, 2, 9, 3, 7]
console.log(num)
num.sort()
console.log(`o vetor ${num} organizado`)
console.log(`O vetor tem ${num.length} posicoes`)


// Caso eu queria substituir um valor de um vetor por outro, posso usar =>  num[3] = 6

// Caso eu queira adicionar um valor ao vetor e nao me preocupar em falar a posicao dele, posso usar =>  num.push(7)

// Caso eu queira ver o comprimento do arry, posso usar o =>  num.lenght

// Caso eu queira organizar todos os elementos em ordem crescente, posso usar o =>  num.sort()

// Caso eu queria buscar um valor dentro de um vetor, posso usar o =>  num.indexOf()